[pytest]
testpaths = tests
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s %(levelname)s %(message)s
markers =
    unit: Tests unitaires sans dependances externes
    integration: Tests avec base de donnees reelle
    precision: Tests de precision numerique Decimal
    security: Tests de securite et validation
